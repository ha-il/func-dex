import playground from "../playground";

describe("playground", () => {
  it("플레이그라운드 테스트", () => {
    // Replace the next dummy test with your playground function tests.
    expect(playground()).toBe(4);
  });
});
